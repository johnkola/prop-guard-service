# PropGuard Service Environment Configuration
# Copy to .env and update values for your environment

#############################
# REQUIRED VARIABLES
#############################

# JWT Configuration (REQUIRED)
JWT_SECRET=your-jwt-secret-key-must-be-at-least-32-characters-long
JWT_EXPIRY_HOURS=24

# Vault Encryption (REQUIRED) 
VAULT_MASTER_KEY=your-32-byte-encryption-master-key-here

#############################
# OPTIONAL VARIABLES
#############################

# Server Configuration
SERVER_PORT=8080
GIN_MODE=release

# BadgerDB Configuration
BADGER_DIR=/app/data
BADGER_ENCRYPTION_ENABLED=false

# CORS Configuration  
CORS_ALLOWED_ORIGINS=http://localhost:3000,http://localhost:3001,http://localhost:8080

# Audit Configuration
AUDIT_RETENTION_DAYS=90

# Logging
LOG_LEVEL=info
LOG_FORMAT=json

#############################
# DEVELOPMENT DEFAULTS
#############################
# For development, you can use these test values:
# JWT_SECRET=test-jwt-secret-exactly-32b
# VAULT_MASTER_KEY=12345678901234567890123456789012

#############################
# DOCKER ENVIRONMENT  
#############################
# When using Docker Compose, these are set automatically:
# BADGER_DIR=/app/data (mounted volume)
# SERVER_PORT=8080 (container port)